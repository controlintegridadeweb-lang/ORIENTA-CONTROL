// Stub vazio para o pacote `server-only` em ambiente de teste (vitest).
// `server-only` existe apenas para falhar o build se um módulo de servidor for
// importado no client; em testes node esse split não existe. Ver vitest.config.ts.
export {};
